# SentimentAnalysis
Built an ML-based text classifier using scikit-learn for training and vectorization. Exposed the model via a FastAPI REST API and created a Java AWT GUI for user interaction. Demonstrates the full ML workflow: preprocessing, model training, API integration, and deployment(on render).
